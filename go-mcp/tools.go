// Generated by sdkgen go-mcp target.
package main

import (
	"context"
	"encoding/json"
	"fmt"
	"strings"

	"github.com/modelcontextprotocol/go-sdk/mcp"
	sdk "github.com/voxgig-sdk/hubspot-communication-preferences-sdk/go"
)

// Args is the common argument shape for both tools. `entity` selects
// the SDK entity to operate on; `query` is the optional reqmatch /
// reqdata map passed through to the SDK. For load, `query` should be
// `{"id": <value>}`. For list, omit `query` or pass an empty map.
type Args struct {
	Entity string         `json:"entity" jsonschema:"subscriptions_action_response_with_results_public_status | subscriptions_action_response_with_results_public_wide_status | subscriptions_action_response_with_results_subscription_definition | subscriptions_batch_response_public_bulk_opt_out_from_all | subscriptions_batch_response_public_status | subscriptions_batch_response_public_status_bulk | subscriptions_batch_response_public_wide_status_bulk | subscriptions_link_generation"`
	Query  map[string]any `json:"query,omitempty" jsonschema:"optional match map e.g. {\"id\":1} for load, omit for list"`
}

func registerTools(server *mcp.Server, client *sdk.HubspotCommunicationPreferencesSDK) {
	mcp.AddTool(server, &mcp.Tool{
		Name: "hubspot-communication-preferences_list",
		Description: "List records from HubspotCommunicationPreferences. " +
			"Args: entity (one of the supported SDK entities), query (optional filter map). " +
			"Returns the first page of records as JSON.",
	}, func(ctx context.Context, req *mcp.CallToolRequest, args Args) (*mcp.CallToolResult, any, error) {
		return runOp(client, "list", args)
	})

	mcp.AddTool(server, &mcp.Tool{
		Name: "hubspot-communication-preferences_load",
		Description: "Load a single record from HubspotCommunicationPreferences. " +
			"Args: entity, query ({\"id\":N} required). Returns the record as JSON.",
	}, func(ctx context.Context, req *mcp.CallToolRequest, args Args) (*mcp.CallToolResult, any, error) {
		return runOp(client, "load", args)
	})
}

func runOp(client *sdk.HubspotCommunicationPreferencesSDK, op string, args Args) (*mcp.CallToolResult, any, error) {
	ent, err := entityFor(client, args.Entity)
	if err != nil {
		return toolError(err.Error())
	}

	var result any
	switch op {
	case "list":
		result, err = ent.List(args.Query, nil)
	case "load":
		result, err = ent.Load(args.Query, nil)
	default:
		return toolError(fmt.Sprintf("unknown op %q", op))
	}
	if err != nil {
		return toolError(err.Error())
	}

	// SDK returns *Entity wrappers; unwrap each via .Data() to get a
	// plain map[string]any (or []any of maps for list) suitable for
	// JSON marshalling.
	data := extractData(result)
	body, err := json.MarshalIndent(data, "", "  ")
	if err != nil {
		return toolError(fmt.Sprintf("marshal: %v", err))
	}
	return &mcp.CallToolResult{
		Content: []mcp.Content{
			&mcp.TextContent{Text: string(body)},
		},
	}, data, nil
}

// entityFor dispatches on the lowercase entity name. The generator
// emits one `case "<name>":` per entity defined in the SDK model.
func entityFor(client *sdk.HubspotCommunicationPreferencesSDK, name string) (sdk.HubspotCommunicationPreferencesEntity, error) {
	switch strings.ToLower(name) {
	case "subscriptions_action_response_with_results_public_status":
		return client.SubscriptionsActionResponseWithResultsPublicStatus(nil), nil
	case "subscriptions_action_response_with_results_public_wide_status":
		return client.SubscriptionsActionResponseWithResultsPublicWideStatus(nil), nil
	case "subscriptions_action_response_with_results_subscription_definition":
		return client.SubscriptionsActionResponseWithResultsSubscriptionDefinition(nil), nil
	case "subscriptions_batch_response_public_bulk_opt_out_from_all":
		return client.SubscriptionsBatchResponsePublicBulkOptOutFromAll(nil), nil
	case "subscriptions_batch_response_public_status":
		return client.SubscriptionsBatchResponsePublicStatus(nil), nil
	case "subscriptions_batch_response_public_status_bulk":
		return client.SubscriptionsBatchResponsePublicStatusBulk(nil), nil
	case "subscriptions_batch_response_public_wide_status_bulk":
		return client.SubscriptionsBatchResponsePublicWideStatusBulk(nil), nil
	case "subscriptions_link_generation":
		return client.SubscriptionsLinkGeneration(nil), nil

	}
	return nil, fmt.Errorf("unknown entity %q", name)
}

// extractData walks an SDK result and replaces any sdk.Entity wrapper
// with its underlying record (via .Data()).
func extractData(x any) any {
	switch v := x.(type) {
	case sdk.Entity:
		return extractData(v.Data())
	case []any:
		out := make([]any, len(v))
		for i, e := range v {
			out[i] = extractData(e)
		}
		return out
	case map[string]any:
		out := make(map[string]any, len(v))
		for k, vv := range v {
			out[k] = extractData(vv)
		}
		return out
	}
	return x
}

func toolError(msg string) (*mcp.CallToolResult, any, error) {
	return &mcp.CallToolResult{
		IsError: true,
		Content: []mcp.Content{
			&mcp.TextContent{Text: msg},
		},
	}, nil, nil
}
